{
  "name": "zotero-translation-popup",
  "version": "0.1.12",
  "private": true,
  "description": "A Zotero translation popup plugin for selected paper text.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check bootstrap.js && node --check content/translator.js && node --check content/popup.js && node --check content/preferences.js"
  }
}
