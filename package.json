{
  "name": "scholarmate-zotero",
  "version": "0.1.13",
  "private": true,
  "description": "A Zotero plugin for OpenAI-powered academic reading assistance.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check bootstrap.js && node --check content/translator.js && node --check content/popup.js && node --check content/preferences.js"
  },
  "devDependencies": {
    "katex": "^0.16.22"
  }
}
